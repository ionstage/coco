{
  "name": "coco",
  "version": "0.0.1",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/**/*.css, js/**/*.js' --server",
    "test": "mocha"
  },
  "devDependencies": {
    "browser-sync": "^2.7.12",
    "mocha": "^2.2.5"
  },
  "private": true
}
