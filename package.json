{
  "name": "coco",
  "version": "0.0.1",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/**/*.css, js/**/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "browserify": "^16.2.3"
  },
  "private": true
}
